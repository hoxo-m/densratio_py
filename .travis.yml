language: python
include:
  matrix:
    - python: "2.7"
      env: DEPS="numpy=1.9 scipy=0.16" BUILD_DOCS=false
# command to install dependencies
install:
  - "pip install -r requirements.txt"
# command to run tests
script: py.test
